# 复制为 .env 后按需修改（不复制则使用默认值）
# 默认值与 application-dev.yml 一致，可直接使用

# 数据库 root 密码（与后端 dev 配置保持一致）
DB_PASSWORD=12345678
# 数据库宿主机映射端口（如本机 3306 被占用可改成 3307）
DB_PORT=3306

# 后端访问端口
SERVER_PORT=8080
# 前端访问端口
FRONTEND_PORT=8000
